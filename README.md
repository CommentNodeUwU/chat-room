### Configuration

Create a `config.json` and set up the port to use

```json
{
    "port": 3000
}
```

### Start in development

Start both

- `npm run dev:client`
- `npm run dev:server`

### Build the project

- `npm run build:client`
- `npm run build:server`
